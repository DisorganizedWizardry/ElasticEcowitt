
output {
    elasticsearch {
      index => "ecowitt-%{+YYYY.MM}"
      hosts => ["https://192.168.0.1:9200" ]
      #cacert => "/etc/elastic_certs/ca.crt"
      #user => "user"
      #password => "pass"
    }
}

